# ===============================================
# CONFIGURAÇÕES GERAIS - PRONAS/PCD SYSTEM
# ===============================================
# ATENÇÃO: Este arquivo é para desenvolvimento local.
# NÃO use estas senhas em produção.

# --- Banco de Dados (PostgreSQL) ---
DB_USER=pronas_admin
DB_PASSWORD=PronasPCD2024!@#
DB_NAME=pronas_pcd
DB_HOST=postgres
DB_PORT=5432
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# --- Cache e Filas (Redis) ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}

# --- Autenticação (JWT) ---
# Gere uma chave segura para produção, ex: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- Armazenamento de Objetos (MinIO S3) ---
MINIO_ROOT_USER=pronas_admin
MINIO_ROOT_PASSWORD=PronasMinio2024!@#
S3_ENDPOINT=http://minio:9000

# --- Busca e Indexação (Elasticsearch) ---
ELASTICSEARCH_URL=http://elasticsearch:9200

# --- Monitoramento (Grafana) ---
GRAFANA_USER=admin
GRAFANA_PASSWORD=PronasGrafana2024!

# --- URLs dos Serviços (para comunicação interna) ---
AUTH_SERVICE_URL=http://auth-service:8000
PROJECTS_SERVICE_URL=http://projects-service:8000
INSTITUTIONS_SERVICE_URL=http://institutions-service:8000
AI_SERVICE_URL=http://ai-service:8000

# --- Configurações de Deploy (opcional, para uso em CD) ---
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
